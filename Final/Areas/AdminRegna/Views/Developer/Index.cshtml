@model List<Developers>
@{
    ViewData["Title"] = "Developer";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Developers</h4>
            <a asp-controller="Developer" asp-action="Create" class="btn btn-success">
                Create
            </a>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                Image
                            </th>
                            <th>
                                Name
                            </th>
                            <th>
                                Profession
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Developers t in Model)
                        {
                        <tr>
                            <td>
                                <img src="/images/team/@t.Image" alt="Alternate Text" />
                            </td>
                            <td>
                                @t.Name
                            </td>
                            <td>
                                @t.Profession
                            </td>
                            <td>
                                <a asp-controller="Developer" asp-action="Detail" asp-route-id="@t.Id" class="btn btn-secondary">
                                    <i class="mdi mdi-information"></i>
                                </a>
                                <a asp-controller="Developer" asp-action="Update" asp-route-id="@t.Id" class="btn btn-warning">
                                    <i class="mdi mdi-table-edit"></i>
                                </a>
                                <a asp-controller="Developer" asp-action="Delete" asp-route-id="@t.Id" class="btn btn-danger">
                                    <i class="mdi mdi-delete"></i>
                                </a>
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
